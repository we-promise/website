<!DOCTYPE html>
<html lang="en">
  <head>
    <title><%= content_for?(:title) ? ("#{yield(:title)} - Sure").html_safe : "Sure: The personal finance app for everyone" %></title>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="title" content="<%= content_for?(:title) ? ("#{yield(:title)} - Sure").html_safe : "Sure: The personal finance app for everyone by everyone" %>">
    <meta name="description" content="<%= content_for?(:description) ? (yield(:description)) : "Sure: The personal finance app for everyone by everyone" %>">
    <meta name="keywords" content="personal finance, budgeting, financial tracking, open source, self-hosted">
    <meta name="author" content="Sure">
    <meta name="robots" content="index, follow">
    <meta name="language" content="English">
    <meta name="revisit-after" content="7 days">
    <meta name="distribution" content="global">
    <meta name="rating" content="general">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <link rel="apple-touch-icon" sizes="180x180" href="/apple-touch-icon.png">
    <link rel="icon" type="image/png" sizes="32x32" href="/favicon-32x32.png">
    <link rel="icon" type="image/png" sizes="16x16" href="/favicon-16x16.png">
    <link rel="manifest" href="/site.webmanifest">
    <link rel="mask-icon" href="/safari-pinned-tab.svg" color="#F9F9F9">
    <meta name="msapplication-TileColor" content="#F9F9F9">
    <meta name="theme-color" content="#F9F9F9">
    <!-- Open Graph / Facebook -->
    <meta property="og:type" content="website">
    <meta property="og:url" content="<%= request.original_url %>">
    <meta property="og:title" content="<%= content_for?(:title) ? ("#{yield(:title)} - Sure").html_safe : "Sure" %>">
    <meta property="og:description" content="<%= content_for?(:description) ? (yield(:description)) : "Sure: The personal finance app for everyone by everyone" %>">
    <meta property="og:image" content="<%= image_url(content_for?(:meta_image) ? yield(:meta_image) : "meta.jpg") %>">
    <meta property="og:site_name" content="Sure">
    <link rel="canonical" href="<%= content_for?(:canonical_url) ? yield(:canonical_url) : request.original_url %>" />
    <!-- Twitter -->
    <meta name="twitter:card" content="summary_large_image">
    <meta name="twitter:title" content="<%= content_for?(:title) ? ("#{yield(:title)} - Sure").html_safe : "Sure" %>">
    <meta name="twitter:site" content="@sure">
    <meta name="twitter:creator" content="@sure">
    <meta name="twitter:description" content="<%= content_for?(:description) ? (yield(:description)) : "Sure: The personal finance app for everyone by everyone" %>">
    <meta name="twitter:image" content="<%= image_url(content_for?(:meta_image) ? yield(:meta_image) : "meta.jpg") %>">
    <%= yield :head %>
    <%= yield :schema_data %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Geist+Mono:wght@100..900&family=Geist:wght@100..900&display=swap" rel="stylesheet">
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= combobox_style_tag %>
    <%= javascript_importmap_tags %>
    <%= hotwire_livereload_tags if Rails.env.development? %>
    <%= turbo_refreshes_with method: :morph, scroll: :preserve %>
    <%= render "shared/ga" if Rails.env.production? %>
    <% if Rails.env.production? && (posthog_config = Rails.configuration.x.posthog).try(:api_key).present? %>
      <%= render "shared/posthog", posthog_api_key: posthog_config.api_key, posthog_host: posthog_config.host %>
    <% end %>
  </head>
  <body class="min-h-screen bg-cover bg-no-repeat" style="background-image: url(<%= asset_path('site-bg.svg') %>)">
    <%= render "shared/navbar" %>
    <main class="px-3 mx-auto my-10 md:px-4">
      <%= yield %>
    </main>
    <%= render "shared/footer" %>
  </body>
</html>
